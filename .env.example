GEMINI_API_KEY=your_api_key
GEMINI_MODEL=your_llm_model
GEMINI_FALLBACK_MODELS="string"
GEMINI_SYSTEM_INSTRUCTION="string"

# YouTube (wajib untuk Cloud Run — IP datacenter sering diblokir YouTube)
# Ekspor cookies Netscape dari browser yang sudah login ke YouTube (gunakan ekstensi seperti "Get cookies.txt LOCALLY")
YOUTUBE_COOKIES=
# Proxy residential/rotating (opsional tapi sangat disarankan di production)
YOUTUBE_PROXY=
# Alias proxy (opsional)
PROXY_URL=
HTTP_PROXY=
HTTPS_PROXY=